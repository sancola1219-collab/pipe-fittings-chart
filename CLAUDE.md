# 管徑查詢（管件查詢圖鑑）

單檔 HTML 的管件查詢工具：一張可縮放的「管件圖鑑牆」，15 種常用管件的示意圖＋尺寸標註，
右側說明欄有一般叫法（俗稱）、材質、用途、規範與全口徑尺寸表。

## 接手必讀順序

1. 本檔（CLAUDE.md）
2. [docs/DATA-SOURCES.md](docs/DATA-SOURCES.md) —— 每個數字是哪裡查來的，改資料前先看
3. [lessons/](lessons/) —— 踩過的坑，一課一檔

## 怎麼開

- 直接雙擊 `index.html`（無任何依賴、不用網路）
- 或本機伺服器：`python -m http.server 8811` → http://localhost:8811

## 操作

- 滾輪縮放、拖曳平移（近遠看）、雙擊管件聚焦
- 上方口徑列（1分～6吋）切換後，全部圖上的尺寸值即時更新
- 點管件 → 右側說明欄：一般叫法／用途／材質／規範／全口徑尺寸表
- 「管徑對照表」按鈕 → 台制分 × 英吋 × A 稱呼 × SGP 外徑厚度重量 × ASME 外徑

## 架構（全部都在 index.html，三個區塊由註解分隔）

| 區塊 | 內容 |
|------|------|
| 資料區 | `SIZES`（14 個口徑，含 SGP/ASME 外徑與 B16.9 五個尺寸值）、`MAT`（材質字典）、`FITTINGS`（15 種管件的名稱/俗稱/用途/材質/規範/尺寸表定義） |
| 繪圖區 | `DRAW` 物件：每管件一個畫圖函式（格子區域 380×360）；`pipe()` 雙描邊畫管身、`solid()` 實心件、`threads()` 螺紋、`dimH()/dimV()` 尺寸線 |
| 互動區 | viewBox 縮放平移、口徑列、搜尋（不符合的格子變淡）、說明欄、對照表彈窗 |

## 改資料 SOP

- **改尺寸值**：只動 `SIZES`。`bw` 陣列順序＝`[90°彎頭A, 45°彎頭B, 三通C, 大小頭H, 管帽E]`（索引名在 `BW` 常數）。改完必須在 DATA-SOURCES.md 補來源。
- **新增管件**：`FITTINGS` 加一筆（id/name/en/alias/usage/mats/std/dimCols/dimGet/note）＋ `DRAW` 加同 id 的畫圖函式。格子會自動排版。
- **鐵則：查不到的尺寸就寫「依廠牌」，絕對不要用推算或記憶編數字。** 牙口管件（由令、束節、短節…）長度本來就無統一標準，只標牙規是刻意的，不是缺漏。

## 已知限制

- B16.9 對焊尺寸只涵蓋 1/2"～6"；1/8"～3/8" 顯示「—」（該標準本來就從 1/2" 起）
- 異徑三通支管 M、法蘭 PCD 依組合/壓力等級變化太多，只給查詢方向不給數值
- 圖為示意圖（辨識用），非等比例工程圖
