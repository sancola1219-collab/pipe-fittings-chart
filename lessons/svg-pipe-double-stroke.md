# SVG 畫管子：同一路徑描兩次（深粗＋淺細），彎頭三通都不用算外形

**為什麼**：管路外形（圓弧彎頭、T 型三通）若用 polygon 算外緣內緣座標很繁瑣又容易接不順。
沿「中心線」畫 path，先用深色 stroke-width w+6 畫一次、再用淺色 w 疊一次，
就得到有輪廓的管身，圓弧、分歧自動平順（`pipe()` 函式）。

**怎麼用**：新增管件時，管狀的一律 `pipe(g, 'M..L..A..')` 給中心線就好；
只有實心體（大小頭、管帽）才用 `solid()` 畫輪廓多邊形。
