# AGENTS.md（給 Codex / 其他 AI 接手用）

這是「管件查詢圖鑑」：單檔 HTML（`index.html`）的管件查詢工具，零依賴、直接開瀏覽器即可用。

## 開始工作前

1. 讀 `CLAUDE.md`（完整架構與修改 SOP，本檔只是入口）
2. 讀 `docs/DATA-SOURCES.md`（尺寸數字的來源與驗證日期）
3. 讀 `lessons/` 內所有檔案（每檔一個坑，開頭一行是摘要）

## 核心規則（違反會做壞這個專案）

- **尺寸數字不可以編造。** 每個數字都要有 `docs/DATA-SOURCES.md` 裡的來源；查不到就顯示「依廠牌」或「—」。
- 全部程式都在 `index.html` 一個檔案裡，保持單檔零依賴，不要引入框架、打包器或外部 CDN。
- 資料（`SIZES`、`FITTINGS`）與繪圖（`DRAW`）分離：改資料不要動繪圖函式，反之亦然。
- 繁體中文介面，台灣用語（口徑講「分」與「吋」，俗稱以台灣水電行叫法為準）。

## 驗證方式

改完後開 `index.html`：
1. Console 無錯誤
2. 切換口徑（上方按鈕）→ 圖上尺寸值會變
3. 點每個管件 → 右側說明欄表格完整
4. 滾輪縮放、拖曳平移正常

## 測試

無自動化測試（純靜態單檔）。驗證＝上述手動清單。
